using System;
using System.Data.SqlClient;

public class sqlAccessor
{
    /*
    Add variables to access DB here private

    examples: 
    private string connectionString = "Server=myServerAddress;
    Database=myDataBase;
    UserId=myUsername;
    Password=myPassword;";
    */

    /**
    Checks if the username exists. If the username exists, check if the password
     is correct. If the password is correct, return true, otherwise return false.
     */
    public bool AttemptLogin(string username, string password) {
        // TODO: Implement logic
    }
    
}